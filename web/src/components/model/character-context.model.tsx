export interface CharacterContext {
    id: number;
    promptSystem: string;
    promptRace: string;
    promptGender: string;
    promptClass: string;
    promptDescription: string;
    createdAt?: string;
}