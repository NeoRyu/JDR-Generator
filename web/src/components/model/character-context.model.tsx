export interface CharacterContext {
  id: number;
  promptSystem: string;
  promptRace: string;
  promptGender: string;
  promptClass: string;
  promptDrawStyle: string;
  promptDescription: string;
  createdAt?: string;
}
