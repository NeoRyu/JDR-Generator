apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jdr-generator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "jdr-generator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jdr-generator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
